version: '3.8'

services:
  run-tests:
    build:
      context: ./
      dockerfile: Dockerfile
    healthcheck:
      test: ["CMD", "./healthcheck.sh"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - .:/sequence/
    command: ./sequence/runTests.sh
